- name: docker build
  docker_image: path=/workspace/webstream-framework/Build/{{ item }} name=webstream/{{ item }}
  with_items:
    - memcached
    - redis
    - mysql
    - postgres
    - php-apache
  become: yes
