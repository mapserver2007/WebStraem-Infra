- name: docker build
  shell: docker build /workspace/webstream-framework/Build/{{ item }} -t webstream/{{ item }}
  with_items:
    - mysql
    - postgres
    - sqlite
    - redis
    - memcached
    - php-alpine
    # - php-apache
    - php-cli
  become: yes
